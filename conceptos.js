/**Objetos:**
En programación, un objeto es una instancia de una clase. Representa un "algo" con propiedades y comportamientos. 
Por ejemplo, un objeto 'coche' podría tener propiedades como color, marca, y modelo, y comportamientos (o métodos) como arrancar y detenerse.

**Métodos:**
Un método es una función que está definida dentro de una clase y actúa sobre los objetos de esa clase. 
Es una forma de interactuar con las propiedades de un objeto o de realizar alguna operación específica. 
Por ejemplo, el método 'arrancar' de un objeto 'coche' haría que el coche comience a moverse.

**Funciones:**
Una función es un bloque de código que realiza una tarea específica. 
Puede tomar entradas (llamadas parámetros) y puede devolver un valor como resultado. 
Por ejemplo, una función que suma dos números y devuelve el resultado.

**Clases:**
Una clase es un molde o plantilla a partir de la cual se crean objetos. Define las propiedades (atributos) y comportamientos (métodos) que los objetos creados a partir de ella tendrán. 
Por ejemplo, una clase 'Coche' define qué características y comportamientos tendrán todos los coches.

**Variables:**
Una variable es un espacio en la memoria que se usa para almacenar datos. 
Tiene un nombre asignado para identificarlo y un valor que es la información almacenada.

**Condicionales:**
Los condicionales son estructuras de control que permiten que el programa tome decisiones y ejecute código diferente según ciertas condiciones. 
Por ejemplo, si una variable es mayor que 10, se ejecuta un bloque de código; de lo contrario, se ejecuta otro.

**Bucles:**
Un bucle es una estructura que permite repetir un bloque de código varias veces. 
Por ejemplo, un bucle 'for' que recorre una lista y realiza una acción para cada elemento.

**Arrays (o Arreglos):**
Un array es una estructura de datos que permite almacenar múltiples valores en una sola variable. 
Los valores se almacenan en índices específicos y pueden ser accedidos o modificados a través de estos índices.

**Pilas y Colas:**
Las pilas (stacks) y las colas (queues) son tipos de estructuras de datos que almacenan elementos en un orden particular. 
Las pilas siguen el principio de "último en entrar, primero en salir" (LIFO), mientras que las colas siguen el principio de "primero en entrar, primero en salir" (FIFO).

**Excepciones:**
Las excepciones son eventos que ocurren durante la ejecución de un programa y que pueden interrumpir su flujo normal. 
El manejo de excepciones permite al programa reaccionar de manera controlada ante errores o situaciones inesperadas.
"""*/
